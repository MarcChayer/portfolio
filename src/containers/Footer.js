import { connect } from 'react-redux';
import About from 'src/components/Footer';

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(About);
